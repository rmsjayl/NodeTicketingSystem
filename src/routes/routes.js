const express = require("express");

const router = express.Router();

// const {} = require("")

// router.route("/").post()
// router.route("/:id").get()
// router.route("/:id").delete();
// router.route("/:id").put();
// router.route("/").get();

module.exports = router;